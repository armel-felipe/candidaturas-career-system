#!/usr/bin/env bash
set -eu

echo "macOS local LinkedIn browser mode."
echo "No remote browser gateway dependencies are required on MacBook."
echo "Install the Playwright browser with:"
echo "  npx playwright install chromium"
